#include <iostream>
#include<algorithm>

using namespace std;

void watest(int n, int ar[], int bin[])
{
    int k = 0;
    // sort(ar,ar+n);
    for(int i =0;i<n;i++){
        if();
    }
}

int main()
{
    // your code goes here
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        int ar[n];
        int bin[n];
        for (int i = 0; i < n; i++)
        {
            cin >> ar[i];
        }
        for (int i = 0; i < n; i++)
        {
            cin >> bin[i];
        }

        watest(n, ar, bin);
    }
    return 0;
}
